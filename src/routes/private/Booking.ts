import { Router } from "express";
// import { someBookingApiFunction } from "../../controllers/BookingController";

const router: Router = Router();

// router.post("/someBookingApiFunction", someBookingApiFunction);

export default router;